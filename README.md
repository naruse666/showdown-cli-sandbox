# showdown-cli-sandbox

```sh
cat style.css test.md |showdown makehtml -o out.html -c tasklists
```

PRを出す場合は, `Allow GitHub Actions to create and approve pull requests`をオンにする。  
`Actions > General > Workflow permissions`
